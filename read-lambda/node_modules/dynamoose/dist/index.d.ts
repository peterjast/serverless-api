import "source-map-support/register";
import { Schema, SchemaDefinition } from "dynamoose/dist/Schema";
import { Condition } from "dynamoose/dist/Condition";
import transaction from "dynamoose/dist/Transaction";
import { Document, AnyDocument } from "dynamoose/dist/Document";
import { ModelType } from "dynamoose/dist/General";
declare const _default: {
    model: {
        <T extends Document = AnyDocument>(name: string, schema?: Schema | SchemaDefinition | (Schema | SchemaDefinition)[], options?: import("dynamoose/dist/General").DeepPartial<import("dynamoose/dist/Model").ModelOptions>): ModelType<T>;
        defaults: any;
    };
    Schema: typeof Schema;
    Condition: typeof Condition;
    transaction: typeof transaction;
    aws: {
        sdk: typeof import("aws-sdk");
        ddb: typeof import("dynamoose/dist/aws/ddb");
        converter: typeof import("dynamoose/dist/aws/converter");
    };
    logger: {
        pause: () => void;
        resume: () => void;
        status: () => "active" | "paused";
        providers: {
            set: (provider: any) => void;
            clear: () => void;
            add: (provider: any) => void;
            delete: (id: string | string[]) => void;
            list: () => any[];
        };
    };
    UNDEFINED: symbol;
    THIS: symbol;
    NULL: symbol;
};
export = _default;
